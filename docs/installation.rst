.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install mutant

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mutant
    $ pip install mutant
